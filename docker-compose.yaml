name: gloford

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: gloford
      POSTGRES_PASSWORD: gloford
      POSTGRES_DB: gloford
    ports:
      - "5433:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gloford -d gloford"]
      interval: 5s
      timeout: 3s
      retries: 30

  migrate:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - migrate-node-modules:/app/node_modules
    environment:
      # Migrations + seed run as superuser so DDL + RLS role creation work.
      DATABASE_URL: postgresql://gloford:gloford@db:5432/gloford?sslmode=disable
      DIRECT_URL: postgresql://gloford:gloford@db:5432/gloford?sslmode=disable
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-O2PqgJfqZ3eZk0kSzv6gOQ3aV2lT8WrxKEP0iFnPw9k=}
      SEED_ADMIN_EMAIL: ${SEED_ADMIN_EMAIL:-admin@gloford.org}
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:-change-me-on-first-login}
    depends_on:
      db:
        condition: service_healthy
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache openssl libc6-compat &&
        corepack enable &&
        corepack prepare pnpm@10.0.0 --activate &&
        pnpm install --frozen-lockfile &&
        pnpm prisma migrate deploy &&
        pnpm db:seed

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      AUTH_SECRET: ${AUTH_SECRET:-replace-me-with-openssl-rand-base64-32}
      AUTH_URL: ${AUTH_URL:-http://localhost:3000}
      AUTH_GOOGLE_ID: ${AUTH_GOOGLE_ID:-}
      AUTH_GOOGLE_SECRET: ${AUTH_GOOGLE_SECRET:-}
      # Runtime connects as the non-superuser gloford_app role so RLS
      # policies actually enforce (see migration 30000000000400_rls_app_role).
      # DIRECT_URL stays on the superuser so Prisma can run DDL if needed
      # (e.g., migrate deploy at boot).
      DATABASE_URL: postgresql://gloford_app:gloford_app@db:5432/gloford?sslmode=disable
      DIRECT_URL: postgresql://gloford:gloford@db:5432/gloford?sslmode=disable
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-O2PqgJfqZ3eZk0kSzv6gOQ3aV2lT8WrxKEP0iFnPw9k=}
      INNGEST_EVENT_KEY: ${INNGEST_EVENT_KEY:-}
      INNGEST_SIGNING_KEY: ${INNGEST_SIGNING_KEY:-}
      INNGEST_BASE_URL: http://inngest:8288
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      MAIL_FROM: ${MAIL_FROM:-Gloford <no-reply@gloford.org>}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY:-}
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  inngest:
    image: inngest/inngest:latest
    command: ["inngest", "dev", "-u", "http://app:3000/api/inngest"]
    ports:
      - "8288:8288"
    depends_on:
      - app

volumes:
  db-data:
  migrate-node-modules:
